<header class="navbar navbar-fixed-top navbar">
    <div class="container">
      <%= link_to "Acxplore", root_path, id: "logo" %>
      <nav>
        <ul class="nav navbar-nav navbar-right">
          <% if logged_in? %>
          <%# needs second confirmation
          for whether user is current user: && !current_user?(@user) %>
            <% if current_user.admin? %>
              <li><%= link_to "Users",      users_path %></li>
              <li><%= link_to "Activities", acreation_path %></li>
            <% end %>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                Account <b class="caret"></b>
              </a>
              <ul class="dropdown-menu">
                <li><%= link_to "Profile", current_user %></li>
                <li><%= link_to "Settings", edit_user_path(current_user) %></li>
                <li class="divider"></li>
                <li>
                  <%= link_to "Log out", logout_path, method: "delete" %>
                </li>
              </ul>
            </li>
          <% else %>
            <li><%= link_to "Sign Up",    signup_path %></li>
            <li><%= link_to "Log in",     login_path %></li>
          <% end %>
        </ul>
        <ul class="nav nav-pills">
          <li role="presentation" class="active">
          <%= link_to "Home", root_path %></li>
          <li role="presentation">
          <%= link_to "All", 'google.ca' %></li>
          <li role="presentation">
          <%= link_to "Categories", 'google.ca' %></li>
        </ul>
      </nav>
    </div>
  </header>
